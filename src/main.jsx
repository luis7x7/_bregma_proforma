import React from "react";
import ReactDOM from "react-dom/client";
import Proforma from "./detalle-solicitud.jsx";

import App from "./app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Proforma />
  </React.StrictMode>
);
